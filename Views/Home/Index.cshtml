@model IEnumerable<Employee>

@{
    ViewBag.Title = "Employee List";
}


@*<div class="card-deck">
    @foreach (var employee in Model)
    {
        var photoPath = "~/Images/" + (employee.PhotoPath ?? "noimage.jpg");
        <div class="card m-3" style="min-width:18rem; max-width:30.5%;">
            <div class="card-header">
                <h3>@employee.Name</h3>
            </div>
            <img class="card-img-top imageThumbnail" src="@photoPath" asp-append-version="true"/>
            <div class="card-footer text-center">
                <a asp-controller="home" asp-action="details" asp-route-id="@employee.Id" class="btn btn-primary m-1">View</a>
                <a href="#" class="btn btn-primary m-1">Edit</a>
                <a href="#" class="btn btn-danger m-1">Delete</a>
            </div>
        </div>
    }
</div>*@
<div class="row">
    <div class="col-sm-12">
        <div class="card-deck">
            @foreach (var Employee in Model)
            {
                var photoPath = "~/Images/" + (Employee.PhotoPath ?? "noimage.jpg");

                <div class="col-sm-4">
                    <div class="card m-3">
                        <div class="card-header">
                            <h3 class="text-center">@Employee.Name</h3>
                        </div>
                        <img class="card-img-top imageThumbnail" src=@photoPath alt="Alternate Text" asp-append-version="true" />
                        <div class="card-footer text-center">
                            <a asp-controller="home" asp-action="details" asp-route-id="@Employee.Id" class="btn btn-primary">View</a>
                            <a  asp-controller="home" asp-action="edit" asp-route-id="@Employee.Id" class="btn btn-primary">Edit</a>
                            <a href="#" class="btn btn-danger">Delete</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
